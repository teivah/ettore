mod opcodes;

// use clap::{App as ClapApp, Arg};

fn main() {
    // let args = ClapApp::new("vega")
    //     .arg(
    //         Arg::with_name("file")
    //             .short("f")
    //             .long("file")
    //             .takes_value(true)
    //             .help("RISC file")
    //             .required(true),
    //     )
    //     .get_matches();
    //
    // let file = args.value_of("file").unwrap();
}
